import { Editor } from "obsidian";


export function commentSelection(editor: Editor, sel: string, codeBlockType: string | null, blockComment: boolean): void {
    // Implémentation de commentSelection
}

export function getPosToOffset(editor: Editor, selection: string, blockComment: boolean): [number, number, string] {
    // Implémentation de getPosToOffset
    return [0, 0, ""];
}